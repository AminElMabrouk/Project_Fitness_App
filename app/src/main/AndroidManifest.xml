<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fitness"
        tools:targetApi="31">
        <activity
            android:name=".after_auth.InformationIndexActivity"
            android:exported="false" />
        <activity
            android:name=".HomePageActivity"
            android:exported="false" />
        <activity
            android:name=".after_auth.ChoseGoalActivity"
            android:exported="false" />
        <activity
            android:name=".authentification.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".authentification.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".authentification.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".after_auth.ChooseGoal_HomePage"
            android:exported="true" />
    </application>

</manifest>